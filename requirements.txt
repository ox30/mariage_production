fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
python-multipart==0.0.20
httpx==0.28.1
pyyaml==6.0.2

# EX-GEN-04 — affichage en heure locale (Europe/Zurich). Windows n'a pas de
# base de fuseaux système, et les images Debian « slim » n'en embarquent pas :
# sans ce paquet, ZoneInfo lève ZoneInfoNotFoundError. Vérifié au démarrage
# par config.zone_affichage().
tzdata==2025.1

# Section 4.2 — ORM et migrations. Requêtes paramétrées par construction
# (EX-SEC-01), et un schéma reproductible d'un déploiement à l'autre.
SQLAlchemy==2.0.52
alembic==1.19.1

# EX-SAU-19 — dépôts objets compatibles S3 : buckets Railway et Cloudflare R2.
boto3==1.43.79
# EX-ADM-05 — lecture du classeur d'import des invités. Version non figée à
# dessein : openpyxl ne sert qu'à lire sept colonnes de texte, et l'épingler au
# correctif près ferait plus de bruit de mise à jour que de risque évité.
openpyxl==3.1.5
